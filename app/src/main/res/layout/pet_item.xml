<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="180dp"
    android:layout_margin="8dp"
    app:cardCornerRadius="24dp"
    app:cardElevation="4dp"
    app:cardBackgroundColor="#FFFFFF"
    app:strokeWidth="0dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground">

    <!-- Using a RelativeLayout to place the photo inside the circular frame -->
    <RelativeLayout
        android:id="@+id/pet_info_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:padding="12dp">

        <!--background frame -->
        <View
            android:id="@+id/image_frame"
            android:layout_width="110dp"
            android:layout_height="110dp"
            android:layout_centerHorizontal="true"
            android:background="@drawable/circular_image_frame" />

        <!-- The actual pet photo, placed on top of the frame -->
        <!-- use a CardView to easily clip the image into a circle -->
        <com.google.android.material.card.MaterialCardView
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:layout_alignTop="@id/image_frame"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="5dp"
            app:cardCornerRadius="50dp"
            app:cardElevation="0dp">

            <ImageView
                android:id="@+id/iv_pet_image"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:src="@drawable/ic_pet_placeholder" />

        </com.google.android.material.card.MaterialCardView>

        <!-- The pet's name, positioned below the image frame -->
        <TextView
            android:id="@+id/tv_pet_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/image_frame"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="8dp"
            android:text="Pet Name"
            android:textColor="#5D4037"
            android:textSize="18sp"
            android:textStyle="bold" />

    </RelativeLayout>

</com.google.android.material.card.MaterialCardView>
