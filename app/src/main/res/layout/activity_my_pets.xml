<?xml version="1.0" encoding="utf-8"?>
<!-- FIXED: Changed to RelativeLayout to accommodate a FloatingActionButton -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFF0F5">

    <include layout="@layout/custom_toolbar" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView_pets"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/custom_toolbar"
        android:clipToPadding="false"
        android:padding="8dp" />

    <!-- FIXED: Added a dedicated FloatingActionButton for adding pets -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_add_pet"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_margin="24dp"
        android:contentDescription="Add New Pet"
        app:backgroundTint="#E1BEE7"
        app:srcCompat="@drawable/ic_add"
        app:tint="@color/white" />

</RelativeLayout>
