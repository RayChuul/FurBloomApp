<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".PetApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FurBloomAppMSD"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- All other activities are now correctly referenced -->
        <activity
            android:name=".AddEditReminderActivity"
            android:parentActivityName=".PetDetailActivity"
            android:exported="false" />

        <activity
            android:name=".MyPetsActivity"
            android:parentActivityName=".MainActivity"
            android:exported="false" />

        <activity
            android:name=".ui.AddPetActivity"
            android:parentActivityName=".MyPetsActivity"/>

        <activity
            android:name=".ui.EditPetActivity"
            android:parentActivityName=".PetDetailActivity" />

        <activity
            android:name=".PetDetailActivity"
            android:parentActivityName=".MyPetsActivity" />

    </application>

</manifest>
